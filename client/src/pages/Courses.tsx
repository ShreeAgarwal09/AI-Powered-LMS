import { AppHeader } from "@/components/AppHeader";
import { CourseCard } from "@/components/CourseCard";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Skeleton } from "@/components/ui/skeleton";
import { trpc } from "@/lib/trpc";
import { ArrowUpRight, Compass, Search, SlidersHorizontal, Sparkles } from "lucide-react";
import { useEffect, useState } from "react";
import { useLocation } from "wouter";

export default function Courses() {
  const [location] = useLocation();
  const initialCategory = new URLSearchParams(location.split("?")[1] || "").get("category") || "all";
  const [query, setQuery] = useState(""); const [category, setCategory] = useState(initialCategory); const [level, setLevel] = useState("all"); const [price, setPrice] = useState("all"); const [committedQuery, setCommittedQuery] = useState("");
  useEffect(() => { const timer = setTimeout(() => setCommittedQuery(query), 250); return () => clearTimeout(timer); }, [query]);
  const categories = trpc.catalog.categories.useQuery();
  const catalog = trpc.catalog.list.useQuery({ query: committedQuery || undefined, categoryId: category === "all" ? undefined : Number(category), level: level === "all" ? undefined : level as any, price: price === "all" ? undefined : price as any });
  return <div className="app-shell min-h-screen"><AppHeader /><main><section className="relative overflow-hidden bg-[#0f355a] py-18 text-white"><div className="absolute inset-0 opacity-70 [background-image:radial-gradient(circle_at_70%_10%,rgba(103,184,224,.42),transparent_28%),radial-gradient(circle_at_18%_92%,rgba(213,173,88,.25),transparent_25%)]" /><div className="container relative grid gap-8 py-16 lg:grid-cols-[1.2fr_.8fr] lg:py-20"><div><p className="page-kicker text-[#a9d8f3]">Curated for your next chapter</p><h1 className="mt-4 max-w-2xl font-display text-5xl font-bold leading-[.98] tracking-tight sm:text-6xl">Explore courses that make progress feel inevitable.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-200">Build a focused learning path with practical courses, clear outcomes, and a rhythm that works around your life.</p></div><div className="hidden rounded-3xl border border-white/15 bg-white/10 p-6 backdrop-blur lg:block"><Sparkles className="h-7 w-7 text-[#f4d789]" /><p className="mt-6 text-sm font-bold uppercase tracking-[.14em] text-[#a9d8f3]">Learning, simplified</p><p className="mt-2 font-display text-2xl leading-tight">Choose a skill. Keep a pace. Make it yours.</p><div className="mt-6 flex items-center gap-2 text-sm text-slate-300"><Compass className="h-4 w-4" />Browse all {categories.data?.length || 0} disciplines</div></div></div></section><section className="container -mt-7 relative pb-14"><div className="surface-card grid gap-4 rounded-3xl p-4 lg:grid-cols-[1.5fr_repeat(3,.7fr)]"><div className="relative"><Search className="absolute left-4 top-3.5 h-4 w-4 text-[#54708d]" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search a skill, topic, or instructor" className="h-12 rounded-xl border-slate-200 bg-slate-50/70 pl-11 shadow-none" /></div><div><Label className="sr-only">Category</Label><Select value={category} onValueChange={setCategory}><SelectTrigger className="h-12 rounded-xl border-slate-200"><SelectValue placeholder="Category" /></SelectTrigger><SelectContent><SelectItem value="all">All categories</SelectItem>{categories.data?.map((item: any) => <SelectItem value={String(item.id)} key={item.id}>{item.name}</SelectItem>)}</SelectContent></Select></div><div><Label className="sr-only">Level</Label><Select value={level} onValueChange={setLevel}><SelectTrigger className="h-12 rounded-xl border-slate-200"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">All levels</SelectItem><SelectItem value="beginner">Beginner</SelectItem><SelectItem value="intermediate">Intermediate</SelectItem><SelectItem value="advanced">Advanced</SelectItem></SelectContent></Select></div><div><Label className="sr-only">Price</Label><Select value={price} onValueChange={setPrice}><SelectTrigger className="h-12 rounded-xl border-slate-200"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">Any price</SelectItem><SelectItem value="free">Free</SelectItem><SelectItem value="paid">Paid</SelectItem></SelectContent></Select></div></div><div className="mt-10 flex flex-wrap items-center justify-between gap-4"><div><p className="page-kicker">The full catalog</p><p className="mt-1 text-sm text-slate-500">{catalog.isLoading ? "Finding courses…" : `${catalog.data?.length || 0} carefully structured courses ready to explore`}</p></div><Button variant="outline" className="rounded-xl border-slate-200 bg-white" onClick={() => { setQuery(""); setCategory("all"); setLevel("all"); setPrice("all"); }}><SlidersHorizontal className="mr-2 h-4 w-4" />Reset filters</Button></div>{catalog.isLoading ? <div className="mt-6 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{[1,2,3,4,5,6].map((key) => <Skeleton className="h-[380px] rounded-3xl" key={key} />)}</div> : catalog.data?.length ? <div className="mt-6 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{catalog.data.map((item: any) => <CourseCard item={item} key={item.course.id} />)}</div> : <div className="mt-6 rounded-3xl border border-dashed border-slate-300 bg-white px-5 py-16 text-center"><Search className="mx-auto h-7 w-7 text-[#54708d]" /><h2 className="mt-4 font-display text-2xl font-bold text-[#143a61]">No matching courses yet</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-500">Try broadening the topic or clearing a filter to see more learning paths.</p><Button variant="outline" className="mt-5 rounded-xl" onClick={() => { setQuery(""); setCategory("all"); setLevel("all"); setPrice("all"); }}>Show the full catalog <ArrowUpRight className="ml-2 h-4 w-4" /></Button></div>}</section></main></div>;
}
